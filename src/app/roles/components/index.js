import Vue from 'vue'

export const Roles = Vue.component('roles', require('./Roles.vue').default)
export const RoleNew = Vue.component('role-new', require('./RoleNew.vue').default)
export const RoleView = Vue.component('role-view', require('./RoleView.vue').default)
export const RoleEdit = Vue.component('role-edit', require('./RoleEdit.vue').default)
