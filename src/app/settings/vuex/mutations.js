export const setBanks = (state, payload) => {
    state.banks = payload
}

export const setBrokers = (state, payload) => {
    state.brokers = payload
}

export const setCurrencies = (state, payload) => {
    state.currencies = payload
}
