export const getAllBanks = (state) => {
    return state.banks
}

export const getAllBrokers = (state) => {
    return state.brokers
}

export const getAllCurrencies = (state) => {
    return state.currencies
}
